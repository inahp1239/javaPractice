package interfaces.parent;

public interface Phone {

    // This is a interface class 
    // interface will have only methods not implementations 
    // it is for future reference for going to build or impelemention in future..

    String model();

    String processor();

    int ram();
    
}
